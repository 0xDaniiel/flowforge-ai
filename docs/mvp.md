🛠️ ROADMAP (MVP – No Backend)
STEP 1 — Setup project structure

Create folders for nodes, canvas, stores, simulation, etc.

STEP 2 — Install React Flow

Get the visual graph engine working.

STEP 3 — Create the canvas page

A blank editor where the graph will live.

STEP 4 — Add basic nodes

Start with one simple node (e.g., TextNode).

STEP 5 — Add Zustand store

To store nodes, edges, and editor state.

STEP 6 — Enable drag-and-drop adding of nodes
STEP 7 — Add a right sidebar to edit node settings
STEP 8 — Add a “Run Simulation” button
STEP 9 — Animate execution
